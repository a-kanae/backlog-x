# セキュリティポリシー

## 脆弱性の報告

**脆弱性を見つけた場合は、公開 Issue には書かないでください。** 修正前に内容が公開されると、利用者を危険に晒すことになります。

代わりに **GitHub の Private vulnerability reporting** をご利用ください。

1. このリポジトリの **Security** タブを開く
2. **Report a vulnerability** をクリック
3. 内容を記入して送信

このやり取りは公開されず、メンテナだけが読めます。GitHub アカウントがあれば利用でき、メールアドレスの交換も不要です。

報告の際、次の情報があると助かります。

- 影響を受ける箇所（ファイル・機能）
- 再現手順
- 想定される影響（何が漏れうるか、何を壊せるか）
- 動作環境（ブラウザとそのバージョン、OS）

個人が余暇に開発しているプロジェクトのため、**対応時間の保証はできません**。ただし報告は必ず確認し、可能な範囲で速やかに対処します。

## サポート対象

**最新版（`main` の最新コミット、およびホスト版）のみ**が対象です。過去のバージョンへの修正のバックポートは行いません。

ホスト版（<https://backlog-x-prd.web.app>）を利用している場合、修正は再デプロイによって自動的に反映されます。自分でホストしている場合は、最新版を取得して再ビルド・再デプロイしてください。

## このプロジェクトで特に重要な観点

backlog-x は**利用者の Backlog データと API キーをブラウザ内だけで扱う**ことを設計の根幹にしています。したがって、次に該当する問題は**重大**として扱います。

- **API キーの漏洩** — キーがメモリの外（ローカルストレージ、URL、ログ、例外メッセージなど）に出てしまう経路
- **データの外部送信** — Backlog スペースと配信元以外への通信が発生する経路。CSP（`firebase.json` の `connect-src`）をすり抜ける方法を含みます
- **保存データへの不正アクセス** — 他のオリジンから IndexedDB / OPFS の内容を読める経路
- **取り込んだデータ由来の XSS** — 課題本文・コメント・Wiki の内容がスクリプトとして実行される経路（Backlog 記法や Markdown のレンダリングは特に注意が必要な箇所です）

## 対象外

次のものは脆弱性として扱いません。

- **利用者自身が自分のデータを閲覧できること** — これは本ツールの目的そのものです。認証や権限管理は設計上持ちません（[README](README.md) の「できないこと」を参照）
- **ブラウザのストレージに保存したデータが、同じ端末の同じブラウザを使える人から見えること** — アーカイブはローカルに保存されます。端末やブラウザプロファイルの管理は利用者の責任範囲です
- 実際の攻撃経路が示されていない、スキャンツールの出力のみの報告

## セキュリティに関わる設計上の約束

このプロジェクトでは、以下を**壊さない**ことを開発上の制約としています。これらに反する変更は、機能改善であっても採用しません。

- アクセス解析・エラー収集・テレメトリを導入しない
- Backlog 以外の外部ホストへ通信しない（CDN からのフォントやスクリプトの読み込みを含む）
- API キーを永続化しない
- CSP の `connect-src` を緩めない

詳細は [CLAUDE.md](CLAUDE.md) の「このプロジェクトの鉄則」を参照してください。
